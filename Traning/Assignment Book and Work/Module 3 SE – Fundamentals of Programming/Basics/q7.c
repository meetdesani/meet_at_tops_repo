//find area of a rectangle.
#include<stdio.h>
main(){
	int l,w, area;
	printf("Write a length of a rectangle.");
	scanf("%d", &l);
	printf("Write a width of a rectangle.");
	scanf("%d", &w);
	area = l*w;
	printf("Area: %d", area);
}
